.row
  - @item.errors.each do |key, value|
    = "#{key} #{value}"
    %br

  = form_for @item do |f|
    = f.label :name, "Name of item:"
    = f.text_field :name

    = f.label :description, "Description:"
    = f.text_area :description, class: "review-body"

    = f.submit "Add item"