.row
  - @review.errors.full_messages.each do |value|
    = value
    %br
  %br

  %p
    Review Form for
    = @restaurant.name
  = form_for [@restaurant, @review] do |f|
    = f.label :rating, "Rating:"
    = f.text_field :rating

    = f.label :body, "Description:"
    = f.text_area :body

    = f.hidden_field :restaurant_id, value: @restaurant.id
    = f.submit "Add Review"